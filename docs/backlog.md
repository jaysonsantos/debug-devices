# Backlog

Open work for debug-devices. One item per request. The orchestrator sends each item to an agent through herdr at once, as a separate brief.

| # | Item | Area | Owner | Status |
|---|---|---|---|---|

Done items move to the commit history. Done: agent guidance for `phone_zoom` and fresh photos (`16cc315`), the full-screen wheel zoom and the snapshot flips (the commit after `99c1b8d`).

No open items.
